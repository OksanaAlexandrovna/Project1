package evidence

class FreezingTemperatures() : Evidence("Freezing Temperatures") {

    fun freezingTemperatures() {
        println(name)
        println("sub-zero temperature in the ghost room") // минусовая температура в комнате призрака
    }
}