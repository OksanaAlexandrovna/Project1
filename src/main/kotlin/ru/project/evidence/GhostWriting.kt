package ru.project.evidence

import ru.project.evidence.Evidence

class GhostWriting() : Evidence("Ghost Writing") {
    fun ghostWriting() {
        println("the ghost left a note in a notebook") // призрак оставил запись в блокноте
    }
}